**Ajax**
就是异步的JavaScript和XML
*优点：无刷新和获取数据，Ajxa向浏览器发送大量的异步请求*

**XML**
可拓展标记语言，用来存储和传输数据，XML和HTML类似，但是HTML是展示数据，XML是存储数据
重启文件时需打开端口：node + js的文件名，如 node server.js
--.js文件数据更换需要重启端口，即重启整个node过程--
**IE缓存问题**
浏览器IE会对AJAX的请求做个缓存，再次发送请求时，走的是本地缓存，而不是服务器，所以需要设置请求头
**5-超时与网络异常**
不能保证服务器快速或网络异常返回信息
